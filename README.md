# Генератор описаний для Handmade

Веб-приложение для автоматической генерации привлекательных описаний handmade изделий с использованием AI.

## Возможности

- 🤖 AI-генерация продающих описаний на русском языке
- 📸 Анализ фотографий изделий
- 💬 Интерактивный чат-интерфейс
- ⭐ Интеграция с Telegram Stars для монетизации
- 🔒 Paywall для контроля доступа

## Архитектура

Приложение состоит из двух частей:
- **Frontend**: React + TypeScript + Vite (развернут на Render)
- **Backend**: Node.js + Express (развернут на Render)

## Telegram Stars Payment Integration

Приложение интегрировано с Telegram Bot API для приема платежей через Telegram Stars.

### Как это работает:

1. Пользователь открывает приложение через Telegram Mini App
2. Если пользователь еще не оплатил доступ, показывается paywall
3. Пользователь нажимает "Оплатить 100 ⭐"
4. Открывается нативный интерфейс оплаты Telegram
5. После успешной оплаты пользователь получает доступ к приложению

### Настройка платежей:

#### 1. Создание бота и получение токена

1. Создайте бота через [@BotFather](https://t.me/botfather)
2. Получите токен бота
3. Настройте бота для приема платежей через Telegram Stars

#### 2. Настройка Backend

Создайте `.env` файл (используйте `.env.example` как шаблон) и добавьте следующие переменные окружения на Render (или другой хостинг):

```env
API_KEY=<your_google_genai_api_key>
TELEGRAM_BOT_TOKEN=<your_telegram_bot_token>
TELEGRAM_WEBHOOK_SECRET=<your_random_webhook_secret>  # Рекомендуется для безопасности
PORT=3001
```

#### 3. Настройка Frontend

Создайте `.env.local` файл (используйте `.env.example.frontend` как шаблон) и добавьте переменную окружения:

```env
VITE_BACKEND_URL=https://ai-mai-backend.onrender.com
```

#### 4. Настройка Webhook (опционально)

Для получения уведомлений о платежах настройте webhook:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://ai-mai-backend.onrender.com/telegram-webhook"}'
```

### API Endpoints

#### POST /create-invoice
Создает счет для оплаты через Telegram Stars.

**Request:**
```json
{
  "userId": 123456789,
  "initData": "telegram_init_data_string"
}
```

**Response:**
```json
{
  "invoiceLink": "https://t.me/invoice/..."
}
```

#### POST /verify-payment
Проверяет статус оплаты пользователя.

**Request:**
```json
{
  "userId": 123456789,
  "initData": "telegram_init_data_string"
}
```

**Response:**
```json
{
  "hasPaid": true
}
```

#### POST /check-payment-status
Проверяет, оплатил ли пользователь доступ.

**Request:**
```json
{
  "userId": 123456789,
  "initData": "telegram_init_data_string"
}
```

**Response:**
```json
{
  "hasPaid": true
}
```

#### POST /telegram-webhook
Webhook для получения уведомлений от Telegram о платежах.

## Развертывание

### Frontend (Render Static Site)

1. Подключите репозиторий к Render
2. Настройте Build Command: `npm install && npm run build`
3. Настройте Publish Directory: `dist`
4. Добавьте переменную окружения `VITE_BACKEND_URL`

### Backend (Render Web Service)

1. Подключите репозиторий к Render
2. Настройте Build Command: `npm install`
3. Настройте Start Command: `node server.js`
4. Добавьте переменные окружения:
   - `API_KEY`
   - `TELEGRAM_BOT_TOKEN`
   - `PORT` (обычно устанавливается автоматически)

## Локальная разработка

### Установка зависимостей

```bash
npm install
```

### Запуск frontend (development mode)

```bash
npm run dev
```

### Запуск backend

```bash
node server.js
```

### Сборка для production

```bash
npm run build
```

## Безопасность

- ✅ Telegram init data проверяется на сервере с использованием HMAC
- ✅ Платежи обрабатываются нативным API Telegram
- ✅ Токены и ключи хранятся в переменных окружения
- ⚠️ **ВАЖНО**: В текущей реализации используется in-memory хранилище для статусов оплаты
  - Данные теряются при перезапуске сервера
  - Перед production развертыванием замените на постоянную базу данных (PostgreSQL, MongoDB и т.д.)
- 🔒 Рекомендуется настроить `TELEGRAM_WEBHOOK_SECRET` для верификации webhook запросов

### Настройка webhook secret (рекомендуется):

1. Сгенерируйте случайный секретный токен
2. Добавьте `TELEGRAM_WEBHOOK_SECRET` в переменные окружения
3. Установите webhook с секретом:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://ai-mai-backend.onrender.com/telegram-webhook",
    "secret_token": "<YOUR_SECRET_TOKEN>"
  }'
```

## Технологии

- **Frontend**: React 19, TypeScript, Tailwind CSS, Vite
- **Backend**: Node.js, Express, Google GenAI
- **Payment**: Telegram Bot API, Telegram Stars
- **Deployment**: Render

## Стоимость

Единоразовая оплата: **100 Telegram Stars** (≈ $2 USD)

Пользователь получает:
- ✅ Неограниченная генерация описаний
- ✅ Качественные продающие тексты на русском
- ✅ AI-анализ фотографий изделий
- ✅ Пожизненный доступ

## Лицензия

MIT

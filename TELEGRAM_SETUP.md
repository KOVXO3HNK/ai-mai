# Telegram Bot Setup Guide

Пошаговая инструкция по настройке Telegram бота для приема платежей через Stars.

## 1. Создание бота

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя для вашего бота (например, "Handmade Description Bot")
   - Введите username для бота (должен заканчиваться на `bot`, например `handmadedescriptionbot`)
4. Сохраните токен бота (будет выглядеть как `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

## 2. Настройка платежей через Stars

1. В чате с @BotFather отправьте `/mybots`
2. Выберите вашего бота
3. Выберите "Bot Settings" → "Payments"
4. Выберите "Telegram Stars"
5. Следуйте инструкциям для активации приема платежей

## 3. Настройка Mini App

1. В чате с @BotFather отправьте `/mybots`
2. Выберите вашего бота
3. Выберите "Bot Settings" → "Menu Button"
4. Настройте Web App URL: `https://ai-mai-bqyd.onrender.com/`

## 4. Настройка Webhook

### Опция 1: Через curl

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://ai-mai-backend.onrender.com/telegram-webhook",
    "secret_token": "<YOUR_RANDOM_SECRET_TOKEN>"
  }'
```

### Опция 2: Через браузер

Откройте в браузере:
```
https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=https://ai-mai-backend.onrender.com/telegram-webhook&secret_token=<YOUR_RANDOM_SECRET_TOKEN>
```

### Проверка webhook

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

## 5. Настройка переменных окружения

### Backend (Render Web Service)

В настройках вашего Web Service на Render добавьте:

```
API_KEY=<ваш_google_genai_api_key>
TELEGRAM_BOT_TOKEN=<токен_бота_из_шага_1>
TELEGRAM_WEBHOOK_SECRET=<случайная_строка_из_шага_4>
```

### Frontend (Render Static Site)

В настройках вашего Static Site на Render добавьте:

```
VITE_BACKEND_URL=https://ai-mai-backend.onrender.com
```

## 6. Тестирование

1. Откройте бота в Telegram
2. Нажмите на кнопку Menu или отправьте команду для открытия Web App
3. Должен открыться экран paywall
4. Нажмите "Оплатить 100 ⭐"
5. Завершите оплату
6. После успешной оплаты вы должны получить доступ к приложению

## 7. Проверка логов

### Проверка логов backend на Render:

1. Зайдите в Dashboard Render
2. Откройте ваш Web Service
3. Перейдите на вкладку "Logs"
4. При успешной оплате вы должны увидеть:
   ```
   User <user_id> has paid successfully
   ```

## Устранение неполадок

### Paywall не показывается
- Проверьте, что приложение открывается через Telegram Web App
- В режиме разработки paywall автоматически пропускается

### Ошибка "Payment system not configured"
- Убедитесь, что `TELEGRAM_BOT_TOKEN` установлен в переменных окружения
- Проверьте, что токен правильный

### Ошибка "Invalid Telegram data"
- Убедитесь, что приложение открывается через Telegram
- Проверьте, что `TELEGRAM_BOT_TOKEN` правильный

### Платеж не обрабатывается
- Проверьте, что webhook настроен правильно
- Проверьте логи backend на наличие ошибок
- Убедитесь, что `TELEGRAM_WEBHOOK_SECRET` совпадает между конфигурацией webhook и переменными окружения

### Доступ теряется после перезапуска сервера
- Это ожидаемое поведение с in-memory хранилищем
- Для production замените на постоянную базу данных

## Важные замечания

⚠️ **ВАЖНО**: Текущая реализация использует in-memory хранилище для статусов оплаты. Данные будут потеряны при перезапуске сервера. Перед production развертыванием обязательно внедрите постоянное хранилище (PostgreSQL, MongoDB и т.д.).

🔒 **БЕЗОПАСНОСТЬ**: Всегда используйте `TELEGRAM_WEBHOOK_SECRET` для защиты от несанкционированных запросов к webhook.

💡 **СОВЕТ**: Сохраните все токены и секреты в безопасном месте (например, в менеджере паролей).
